import rtconfig
Import('RTT_ROOT')
from building import *

# get current directory
cwd = GetCurrentDir()

# The set of source files associated with this SConscript file.
src = []
src += Glob('*.c')

group = DefineGroup('CH32_CDC', src, depend = [''], CPPPATH = [cwd])

Return('group')
